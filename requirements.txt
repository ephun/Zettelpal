# Zettelpal dependencies are declared in pyproject.toml.
# Install the project (and its dependencies) with:
#   pip install -e .
# Or with dev extras (pyinstaller, pytest):
#   pip install -e .[dev]
-e .
